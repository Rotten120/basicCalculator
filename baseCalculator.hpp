#include <string>
#include <vector>

enum OP {ADD, SUB, MUL, DIV, EXP};

void readSyntax(std::string buffer) {
    const unsigned short int bufferSize = buffer.size();
    std::vector<int> inputs;

    for(int i = 0; i < bufferSize; i++) {

    }
}
